# 🏛️ Arquitectura del Sistema

## Arquitectura General

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                   CLIENTE                                       │
│                    (Frontend React + Socket.io Client)                          │
└─────────────────────────────────────┬───────────────────────────────────────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │ HTTP (REST API)    │ WebSocket    │
                    ▼                    ▼              │
┌───────────────────────────────────────────────────────────────────────────────┐
│                              EXPRESS SERVER                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐   │
│  │   Routes    │  │ Controllers │  │ Middlewares │  │   Socket.io Server  │   │
│  │  (API v1)   │──│  (Lógica)   │──│ (Auth, Rate │  │  (Notificaciones    │   │
│  │             │  │             │  │  Limiting)  │  │   en tiempo real)   │   │
│  └─────────────┘  └──────┬──────┘  └─────────────┘  └──────────┬──────────┘   │
│                          │                                      │              │
│  ┌───────────────────────┴──────────────────────────────────────┘              │
│  │                                                                             │
│  ▼                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                           SERVICES LAYER                                │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │   │
│  │  │ Notification │  │   RabbitMQ   │  │    Email     │  │   Stripe    │  │   │
│  │  │   Service    │  │   Service    │  │   Service    │  │   Service   │  │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │                              JOBS (Cron)                                │   │
│  │  ┌────────────────────────────────────────────────────────────────────┐ │   │
│  │  │ cleanupInactiveAccounts (diario 3:00 AM) - Elimina cuentas viejas  │ │   │
│  │  └────────────────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────┬───────────────────────────────────────────┘
                                     │
         ┌───────────────────────────┼───────────────────────────┐
         │                           │                           │
         ▼                           ▼                           ▼
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│    MongoDB      │       │    RabbitMQ     │       │     Stripe      │
│   (Mongoose)    │       │   (Mensajería)  │       │    (Pagos)      │
│                 │       │                 │       │                 │
│ • Users         │       │ Exchange:       │       │ • Checkout      │
│ • Transactions  │       │   notifications │       │ • Webhooks      │
│ • Notifications │       │                 │       │ • Suscripciones │
│ • Categories    │       │                 │       │                 │
└─────────────────┘       └─────────────────┘       └─────────────────┘
```

## Arquitectura AWS (Producción)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              INTERNET                                           │
└─────────────────────────────────────┬───────────────────────────────────────────┘
                                      │
                                      ▼
                        ┌─────────────────────────┐
                        │       AWS WAF           │
                        │  (Web Application       │
                        │   Firewall)             │
                        │  • Rate limiting        │
                        │  • SQL injection        │
                        │  • XSS protection       │
                        │  • Bot control          │
                        └────────────┬────────────┘
                                     │
                                     ▼
                        ┌─────────────────────────┐
                        │   Application Load      │
                        │      Balancer (ALB)     │
                        │                         │
                        │  api.nizerapp.net       │
                        │  (HTTPS/443 + WSS)      │
                        └────────────┬────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           AWS ECS FARGATE                                       │
│  ┌───────────────────────────────────────────────────────────────────────────┐  │
│  │                    Task Definition (1 vCPU / 2GB RAM)                     │  │
│  │  ┌─────────────────────────────┐  ┌─────────────────────────────────────┐ │  │
│  │  │  expense-controller-api     │  │  aws-otel-collector (sidecar)       │ │  │
│  │  │  (Node.js + Express)        │  │  • CloudWatch Logs                  │ │  │
│  │  │  Puerto: 5000               │  │  • X-Ray Tracing                    │ │  │
│  │  └─────────────────────────────┘  └─────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────┘
                                     │
         ┌───────────────────────────┼───────────────────────────┐
         │                           │                           │
         ▼                           ▼                           ▼
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│  AWS Secrets    │       │   Amazon ECR    │       │  CloudWatch     │
│    Manager      │       │  (Container     │       │    Logs         │
│                 │       │   Registry)     │       │                 │
│ • JWT_SECRET    │       │ expense/backend │       │ /ecs/expense-   │
│ • MONGO_URI     │       │ :latest         │       │ controller-...  │
└─────────────────┘       └─────────────────┘       └─────────────────┘
```

## Configuración de la Task Definition (ECS Fargate)

| Parámetro | Valor |
|-----------|-------|
| **CPU** | 1024 (1 vCPU) |
| **Memoria** | 2048 MB |
| **Plataforma** | Linux/X86_64 |
| **Compatibilidad** | FARGATE |
| **Región** | mx-central-1 |

### Contenedores

1. **expense-controller-api** (principal)
   - Imagen: `118424916564.dkr.ecr.mx-central-1.amazonaws.com/expense/backend:latest`
   - Puerto: 5000
   - Variables de entorno: `NODE_ENV=production`, `PORT=5000`, `JWT_EXPIRES_IN=8h`
   - Secretos desde AWS Secrets Manager: `JWT_SECRET`, `MONGO_URI`

2. **aws-otel-collector** (sidecar)
   - Imagen: `public.ecr.aws/aws-observability/aws-otel-collector:v0.45.1`
   - Recolección de métricas y trazas para CloudWatch y X-Ray

## Seguridad

### Capas de Seguridad en AWS

| Capa | Servicio | Protección |
|------|----------|------------|
| **Perímetro** | AWS WAF | Rate limiting, SQL injection, XSS, bot control |
| **Red** | ALB + Security Groups | Solo tráfico HTTPS/443, WebSocket seguro |
| **Secretos** | AWS Secrets Manager | Credenciales encriptadas y rotación automática |
| **IAM** | Task Role | Permisos mínimos necesarios (least privilege) |
| **Logs** | CloudWatch | Auditoría y monitoreo centralizado |
| **Trazabilidad** | AWS X-Ray | Trazas distribuidas para debugging |

### Seguridad a Nivel de Aplicación

| Característica | Descripción |
|----------------|-------------|
| HTTP-only Cookies | Tokens JWT en cookies seguras (previene XSS) |
| JWT | Autenticación con expiración configurable |
| bcrypt | Hash de contraseñas |
| Rate Limiting | Prevención de ataques de fuerza bruta |
| Zod | Validación estricta de datos |
| Helmet | Headers de seguridad HTTP |
| CORS | Configurado con `credentials: true` |
| Protección IDOR | Verificación de propiedad en recursos |

## Dominio y SSL

- **Frontend**: `https://nizerapp.net`
- **API**: `https://api.nizerapp.net` (detrás del ALB)
- **Certificado SSL**: AWS Certificate Manager (ACM)

## Estructura del Proyecto

```
backend/
├── src/
│   ├── controllers/          # Controladores de rutas
│   ├── interfaces/           # Interfaces TypeScript
│   ├── jobs/                 # Tareas programadas (cron jobs)
│   ├── models/               # Modelos de MongoDB
│   ├── routes/               # Definición de rutas
│   ├── services/             # Lógica de negocio
│   │   ├── notifications/    # Servicios de notificaciones
│   │   ├── consumers/        # Consumidores de mensajes
│   │   └── websocket/        # WebSocket server
│   ├── middlewares/          # Middlewares personalizados
│   ├── config/               # Configuración centralizada
│   ├── utils/                # Utilidades (logger, etc.)
│   ├── app.ts                # Configuración de Express
│   ├── swagger.ts            # Configuración de Swagger/OpenAPI
│   └── server.ts             # Punto de entrada
├── docs/                     # Documentación del proyecto
│   ├── arquitectura.md       # Este archivo
│   ├── api.md                # Referencia de API
│   ├── notificaciones.md     # Sistema de notificaciones
│   ├── suscripciones.md      # Integración Stripe
│   └── cron-jobs.md          # Tareas programadas
├── .aws/                     # Configuración AWS
│   └── task-definitions.json # Task definition para ECS Fargate
├── defaultCategories.json    # Categorías predefinidas del sistema
├── Dockerfile                # Imagen Docker
├── env.example               # Variables de entorno de ejemplo
├── package.json
└── tsconfig.json
```

## Logger Personalizado

El sistema incluye un logger con colores y formato enriquecido ubicado en `src/utils/logger.ts`.

```typescript
import { logger } from './utils/logger';

logger.info('Mensaje informativo');      // Cyan
logger.success('Operación exitosa');     // Verde
logger.debug('Datos de depuración', {}); // Gris
logger.warn('Advertencia importante');   // Amarillo
logger.error('Error crítico', error);    // Rojo
```

### Formato de salida

```
[ 2025-01-15 14:30:45 UTC-6 | server.ts           ] INFO   : Servidor iniciado en puerto 5000
```
